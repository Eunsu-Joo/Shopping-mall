import cartResolver from "./cart";
import productResolver from "./product";
export default [productResolver, cartResolver];

/* 🚀 Resolvers
   => Query, Mutation, Subscription 과 같은 선언된 타입의 정의 부분
* **/
// (obj, args, context,info)
